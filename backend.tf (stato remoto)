terraform {
  backend "s3" {
    bucket         = "terraform-state-giovanni"
    key            = "devops/terraform.tfstate"
    region         = "eu-west-1"
    dynamodb_table = "terraform-lock"
    encrypt        = true
  }
}
